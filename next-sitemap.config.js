   /** @type {import('next-sitemap').IConfig} */
   const config = {
    siteUrl: 'https://noktaelektronik.net', // Your site's URL
    generateRobotsTxt: true, // Generate robots.txt file
    // Additional options can be added here
  };

  module.exports = config;